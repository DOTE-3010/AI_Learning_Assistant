Develop a one-pass implementation plan for multi-turn iterative assignment generation with explicit context-window statistics. Keep the architecture decoupled and adapter-based: use LangChain as the default context manager, but preserve stack swappability via interfaces and a technical-description swap point. The plan must target the current codebase , minimize unrelated refactors, preserve existing API behavior unless required by this feature, and include concrete file-level changes, dependency updates, rollout steps, and verification steps that can be executed in one implementation run. The plan must explicitly include regression prevention for the integrated demo UI flow: (1) newly created assignments must appear immediately in the sidebar, (2) context window statistics panel must be persistently visible and update from local estimate and backend polling, (3) input box and attachment label must clear after generation request is accepted, and (4) frontend source changes must be built into backend static assets used by `/ui`. Save the roadmap as roadmap_context_management.md.