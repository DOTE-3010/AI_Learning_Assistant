# Desktop Shell

Electron shell placeholder for the phase-1 Docker Desktop runtime.

This package will own Docker Desktop detection, backend health polling, window lifecycle, and reveal-in-folder behavior. It must communicate with the backend only through HTTP/SSE.
