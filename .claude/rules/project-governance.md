---
description: Broad project governance for Claude Code
---

`AGENTS.md` is canonical. Do not duplicate long project rules here.

Phase-1 development is complete. The task queue (`docs/TASKS/`) is empty. The product uses HTML-native artifact generation with Playwright HTML-to-PDF. Read `docs/IMPLEMENTATION_SUMMARY.md` for the completed development ledger.

For frontend work, treat `docs/CONTRACTS/ui-workbench.md` and `docs/CONTRACTS/visual-assets.md` as required reading. The UI is a preview-only conversational workbench: production console beside artifact preview.

If a change requires touching more than its declared scope, update the governance asset or ask the human.
