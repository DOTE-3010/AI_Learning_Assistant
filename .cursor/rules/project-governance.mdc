---
description: Project governance for AI Learning Assistant
alwaysApply: true
---

`AGENTS.md` is canonical. Read it before any implementation work.

Phase-1 development is complete. The product uses HTML-native artifact generation with Playwright HTML-to-PDF (see `docs/DECISIONS/009-html-native-artifact-generation.md`). There is no LaTeX dependency. The task queue (`docs/TASKS/`) is empty.

For frontend work, read `docs/CONTRACTS/ui-workbench.md` and `docs/CONTRACTS/visual-assets.md`. The UI is a preview-only conversational artifact workbench: production console beside a persistent artifact preview panel.

Keep changes bounded. If a fix needs to change a contract, update the governance asset first or ask the human.
