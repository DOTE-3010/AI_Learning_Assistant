<!--
Owner: project-maintainer
Last Reviewed: 2026-06-05
Status: Active
-->

# Implementation Completion Summary

## Purpose

This file is the development-completion ledger for the phase-1 rebuild. It records what the completed task queue implemented from the governance files, how it was implemented at the module level, and which repository artifacts now carry the behavior.

This is a QA handoff, not a QA certification. Whole-product correctness must still be validated through `docs/QA_PLAN.md` and the active QA tasks in `docs/TASKS/`.

## Completion Basis

- The human has declared development complete and asked to move the project into whole-product QA.
- `docs/TASKS/README.md` previously recorded implementation tasks `000`-`018` as complete and tasks `019`, `020`, `021`, `022`, and `023` as landed in the current workspace.
- The old numbered implementation tasks have been removed from the active queue so future agents do not treat them as pending work.
- Human review on 2026-06-03 promoted the missing backend upload API from a known QA risk to a pre-QA blocker. That blocker was resolved on 2026-06-03, agent QA proceeded, and on 2026-06-05 the human completed manual E2E functional testing. All four generation capabilities passed; active work now consists of post-human-E2E repair tasks for status/progress, real previews, LaTeX diagram handling, performance triage, and course context.

## Implemented Governance Claims

| Governance promise | How it was implemented | Repository artifacts |
| --- | --- | --- |
| Local-first Electron plus Docker runtime for phase 1 | The runtime uses Docker Compose for the backend container, mounted local `data/` and `workspace/` paths, and an Electron shell that detects/starts the Docker-backed workbench over HTTP. | `compose.yml`, `Dockerfile`, `apps/desktop/`, `run_desktop.command`, `run_desktop.bat`, `README.md` |
| SQLite as the local metadata store | Backend storage was moved to a SQLite repository foundation with schema initialization and repositories for users, sessions, model profiles, runs, uploads, artifacts, and citations. | `backend/storage/sqlite.py`, `backend/tests/test_sqlite_storage.py`, `docs/CONTRACTS/sqlite-schema.md` |
| Generated bytes live on disk, not in SQLite | Artifact creation writes run folders, input notes, output files, logs, and manifests under the configured workspace root, while SQLite stores metadata and paths. | `backend/artifacts/filesystem.py`, `workspace/`, `backend/tests/test_artifact_filesystem.py`, `docs/CONTRACTS/artifact-filesystem.md` |
| CUHK-style weak auth remains but is isolated for replacement | Registration/login accept `@cuhk.edu.hk` and `@link.cuhk.edu.hk`, issue opaque bearer tokens, and store local identity/session metadata in SQLite. | `backend/api/auth.py`, `backend/core/weak_auth.py`, `backend/tests/test_auth_sqlite.py`, `docs/CONTRACTS/auth.md` |
| Upload ingestion API | Authenticated multipart uploads persist accepted phase-1 file types on disk, store owner-scoped metadata in SQLite, return metadata-only responses, enforce upload limits, and reject unsupported or oversized files through the canonical error envelope. | `backend/api/uploads.py`, `backend/core/uploads.py`, `backend/artifacts/filesystem.py`, `backend/storage/sqlite.py`, `backend/tests/test_uploads_api.py`, `docs/CONTRACTS/uploads.md` |
| Qwen/OpenAI-compatible provider abstraction without tracked secrets | Model settings support user-provided base URL, model, context window hint, streaming support, and API key storage through redacted metadata plus a local secret file/env boundary. Mock and OpenAI-compatible providers are separated. | `backend/api/settings.py`, `backend/core/model_settings.py`, `backend/providers/`, `backend/tests/test_model_settings.py`, `docs/CONTRACTS/model-settings.md` |
| Generation run API with explicit artifact intent | Authenticated run creation/status APIs persist run metadata and require explicit intents: `code_homework`, `essay_latex`, `beamer_slides`, or `cheat_sheet`. | `backend/api/runs.py`, `backend/core/runs.py`, `backend/pipelines/router.py`, `backend/tests/test_runs_api.py`, `docs/CONTRACTS/generation-pipeline.md` |
| Stage status and event reporting | Run lifecycle state, status events, and stage metadata are exposed through backend event/status support and tested against the generation contract. | `backend/core/run_events.py`, `backend/tests/test_run_status_events.py`, `backend/api/runs.py` |
| Context preparation, search policy, and revision budgeting | Context extraction, budget estimation, search-mode decisions, and profile-aware revision source/log allocation are isolated under the backend context layer. | `backend/context/`, `backend/tests/test_intent_router_context.py`, `backend/context/budget.py`, `docs/CONTRACTS/uploads.md`, `docs/CONTRACTS/generation-pipeline.md` |
| Four phase-1 artifact pipelines | Separate pipeline modules generate code homework, essay LaTeX/PDF, Beamer LaTeX/PDF, and dense cheat-sheet LaTeX/PDF outputs through the shared artifact writer and model provider boundary. | `backend/pipelines/code_homework.py`, `essay_latex.py`, `beamer_slides.py`, `cheat_sheet.py`, `backend/tests/test_*_pipeline.py` |
| Preview-first conversational workbench | The frontend presents the authenticated artifact studio, explicit artifact controls, model settings, upload/search/run controls, context dial, localized copy boundary, warm editorial design tokens, and preview-only artifact surfaces. | `frontend/src/app.js`, `frontend/src/styles.css`, `frontend/src/design-tokens.css`, `frontend/src/locales.js`, `frontend/index.html` |
| English, Simplified Chinese, and Traditional Chinese UI copy boundary | Frontend user-facing strings are routed through a locale catalog instead of being only hard-coded English view text. | `frontend/src/locales.js`, `frontend/src/app.js`, `docs/CONTRACTS/ui-workbench.md` |
| Revision runs preserve prior artifacts | Follow-up requests can reference a prior run, create independent revision output, and include bounded sanitized prior context without mutating old artifacts. | `backend/core/runs.py`, `backend/context/budget.py`, `backend/tests/test_runs_api.py`, `docs/CONTRACTS/generation-pipeline.md` |
| End-to-end mocked runtime smoke | A smoke script starts the Docker runtime with mocked model settings, exercises auth, model settings, run creation, status event, manifest creation, and workbench static serving without live credentials. | `scripts/smoke_e2e.sh`, `README.md`, `compose.yml` |
| Governance and cross-agent adapters | Durable governance files define product intent, architecture boundaries, rules, contracts, decisions, implementation summary, and QA sequence. Tool adapters point back to `AGENTS.md` as canonical. | `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/`, `.claude/rules/`, `docs/` |

## Completed Task Ledger

| Former task | Completion summary | Primary output |
| --- | --- | --- |
| `000-bootstrap-local-dev-environment` | Established repo-local backend and frontend verification commands. | `.venv` workflow, `backend/requirements-dev.txt`, frontend lockfile |
| `001-bootstrap-rebuild-skeleton` | Created the canonical phase-1 layout without new `apps/web/` or `services/api/` roots. | `backend/`, `frontend/`, `apps/desktop/`, `workspace/`, `data/` |
| `002-replace-local-storage-with-sqlite` | Introduced SQLite as the local metadata foundation. | `backend/storage/sqlite.py`, storage tests |
| `003-implement-weak-auth-contract` | Implemented CUHK weak registration/login on SQLite with opaque bearer tokens. | Auth API/core modules and tests |
| `004-add-model-profile-settings` | Added redacted model profile settings and provider connectivity support. | Settings API/core/provider modules and tests |
| `005-implement-artifact-filesystem` | Added safe run-folder and manifest writer. | Artifact filesystem module and tests |
| `006-build-generation-run-api` | Added authenticated run creation and status lookup. | Runs API/core modules and tests |
| `007-add-intent-router-and-context-builder` | Added explicit intent routing and context estimation/extraction helpers. | Pipeline router, context modules, tests |
| `008-implement-web-search-policy` | Added `auto`, `on`, and `off` search policy handling and citation boundary. | Context search policy modules |
| `009-implement-code-homework-pipeline` | Added script/notebook-oriented homework output pipeline. | Code homework pipeline and tests |
| `010-implement-essay-latex-pipeline` | Added essay LaTeX source persistence and PDF compile attempt flow. | Essay pipeline and tests |
| `011-implement-beamer-slides-pipeline` | Added Beamer source persistence and PDF compile attempt flow. | Beamer pipeline and tests |
| `012-implement-cheat-sheet-pipeline` | Added dense A4 cheat-sheet pipeline with PDF-upload context expectations. | Cheat-sheet pipeline and tests |
| `013-add-run-status-streaming` | Added run-stage event/status support. | Run events module and tests |
| `014-define-visual-system-and-assets` | Defined warm editorial visual system and asset prompts. | `docs/ASSET_PROMPTS/`, visual contracts |
| `015-rebuild-web-workbench-shell` | Replaced the product surface with the artifact studio shell. | Frontend app/styles |
| `016-add-context-budget-dial` | Added graphical context budget state to the workbench. | Frontend UI/context estimate handling |
| `017-add-model-settings-editor` | Added model settings UI with redacted key handling. | Frontend settings flow |
| `018-redesign-conversational-preview-workbench` | Rebuilt visual shell, preview surfaces, locales, and motion treatment while preserving backend-facing behavior. | Frontend workbench renderer |
| `019-add-revision-run-support` | Added independent revision runs referencing prior outputs. | Runs/context modules and tests |
| `020-scaffold-electron-docker-shell` | Added Electron shell scaffold for Docker detection, startup, health polling, and workbench loading. | `apps/desktop/` |
| `021-update-compose-and-launchers` | Updated Compose and launchers for the rebuilt SQLite-backed runtime. | `compose.yml`, launchers, README |
| `022-end-to-end-smoke-and-legacy-cleanup` | Added mocked E2E smoke coverage and documented the rebuilt runtime as the active path. | `scripts/smoke_e2e.sh`, README/runtime docs |
| `023-add-dynamic-revision-context-budget` | Replaced fixed revision caps with profile-aware bounded context allocation. | `backend/context/budget.py`, related tests |

## Resolved Pre-QA Blocker

- `docs/CONTRACTS/uploads.md` defines `POST /api/uploads`, the frontend has upload controls, and uploads are part of `docs/SPEC.md` phase-1 acceptance criteria. By human decision on 2026-06-03, the missing backend upload API had to be fixed before agent QA could start. The former upload blocker task implemented the upload API and passed its verification commands on 2026-06-03; the task file was later cleared from the active queue as completed history.

## Known QA Risks

- Default Qwen endpoint, model id, and context hint were pinned in `docs/CONTRACTS/model-settings.md` on 2026-06-03 after checking official Alibaba Cloud Model Studio documentation and confirming the release API key belongs to the China site. Live provider smoke still requires untracked credentials and may fail if a user's regional API key does not match the default endpoint.
- Human E2E on 2026-06-03 found a live essay/PDF failure caused first by a missing Docker LaTeX `lmodern` package and then by ordinary generated-source LaTeX syntax. QA task `005` added the runtime dependency and a one-pass model-assisted LaTeX repair/recompile path; old failed runs remain historical records and should be rerun rather than rewritten.
- The concrete web-search provider and rate/cost limits remain undecided. Search policy behavior can be tested with mocks, but live search remains a product/integration risk.
- Native no-Docker packaging, signed macOS app distribution, and hosted deployment remain future-phase work and should not block phase-1 Electron plus Docker QA.
- Real model-provider smoke tests require untracked credentials. Agent-run QA should prefer mocked-provider tests unless the human explicitly supplies local credentials for a real-provider check.

## Post-Human-E2E Findings

- Functional generation for code homework, essay PDFs, Beamer slides, and cheat sheets passed human E2E.
- High-priority blockers remain for truthful run status motion and comfort progress, real generated-output previews, and avoiding failed/non-rendered diagrams in PDFs.
- Medium-priority approved follow-ups cover lightweight course context containers and performance bottleneck triage.
- Low-priority onboarding/tutorial work is recorded in `docs/SPEC.md` but is not an active implementation task.

## Historical Queue Handling

The old implementation task files were intentionally removed from `docs/TASKS/` on 2026-06-03. Completed pre-QA, agent QA, first human E2E fix, and pre-E2E repair task files were cleared from `docs/TASKS/` on 2026-06-05 by human decision. This summary and `docs/QA_REPORTS/` are now the historical references for completed work; active work begins from the numbered post-human-E2E repair queue under `docs/TASKS/`.
